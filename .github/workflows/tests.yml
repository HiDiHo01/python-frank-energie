name: Run Tests

permissions:
  contents: read
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  tests:
    permissions:
      contents: read
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@b09bb98e06d4d774595224525879c09bc6e98c40 # @v2.20.1
        with:
          egress-policy: block
          allowed-endpoints: >
            api.github.com:443
            github.com:443
            pypi.org:443
            files.pythonhosted.org:443
            sonarcloud.io:443
            objects.githubusercontent.com:443
            uploads.github.com:443
            upload.pypi.org:443
            test.pypi.org:443
            codecov.io:443
            uploader.codecov.io:443
            api.codecov.io:443
            cli.codecov.io:443
            storage.googleapis.com:443
            github-releases.githubusercontent.com:443
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # @v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # @v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Poetry
        run: |
          pip install --only-binary :all: poetry==2.4.1
          # No venv caching in this workflow, so install into the runner's
          # Python directly rather than paying for an unused venv.
          poetry config virtualenvs.create false
          poetry config installer.only-binary :all:

      - name: Install dependencies
        run: poetry install --no-interaction --with=dev
        env:
          POETRY_INSTALLER_ONLY_BINARY: ":all:"

      - name: Run pytest with coverage
        run: |
          PYTHONPATH=. poetry run pytest \
            --asyncio-mode=auto \
            --disable-warnings \
            --maxfail=1 \
            --cov=python_frank_energie \
            --cov-report=xml \
            tests/

      - name: Upload coverage report
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # @v7
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
